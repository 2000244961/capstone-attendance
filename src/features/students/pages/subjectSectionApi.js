// Minimal stub to resolve build error
export const fetchSubjectSections = () => Promise.resolve([]);
export const addSubjectSection = (section) => Promise.resolve(section);
export const updateSubjectSection = (section) => Promise.resolve(section);
export const deleteSubjectSection = (id) => Promise.resolve(id);
